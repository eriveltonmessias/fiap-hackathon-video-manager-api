apiVersion: v1
kind: Service
metadata:
  name: video-manager-api
  namespace: ${apps_namespace}
spec:
  type: ${service_type}
  ports:
    - name: http
      port: 8082
      targetPort: http
  selector:
    app.kubernetes.io/name: video-manager-api
